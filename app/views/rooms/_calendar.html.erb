<%= month_calendar events: schedules do |date, schedules| %>
    <%= date %>
    <% schedules.each do |schedule| %>
      <div class="flew-grow schedule">
        <%= link_to schedule.user.name, schedule, class: "link" %>
      </div>
    <% end %>
  <% end %>