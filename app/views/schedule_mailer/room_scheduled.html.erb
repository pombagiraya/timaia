<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <style>
    body {
      font-family: Helvetica, arial, sans-serif;
      font-size: 15px;
      background: #f6f6f6;
      padding: 20px;
    }
  </style>
  <body>

    <table style="background-color: white; width: 600px; margin-left: auto; margin-right: auto; padding: 40px; border: 1px solid #DDDDDD;">
      <tbody>
        <tr>
          <td>
            <table>
              <tr>
                <td style="text-align: center">
                  <%= image_tag("building.png", size: "173x184") %>
                  <h1 style="font-size: 22px; ">Welcome to TiMaia, <%= @user.name %></h1>
                  <p style="font-size: 18px; line-height: 24px;">You have successfully scheduled the <%= @schedule.room.name %> and were charged R$<%= @schedule.room.price %></p>
                </td>
              </tr>
            </table>
            <h3>Schedule Details</h3>

            <table style="display:table; border-collapse: collapse; width: 100%; margin-bottom: 40px;">
              <thead>
                <tr>
                  <th style="text-align: left; font-size: 14px;">Room:</th>
                  <th style="text-align: left; font-size: 14px;">Starts:</th>
                  <th style="text-align: left; font-size: 14px;">Ends:</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><p><%= @schedule.room.name %></p></td>
                  <td><p><%= readable_time(@schedule.start_time) %></p></td>
                  <td><p><%= readable_time(@schedule.end_time) %></p></td>
                </tr>
              </tbody>
            </table>

            <p></p>
            <p>To login to the site, just follow this link: <%= link_to "Login", new_user_session_url %>.</p>
            <p>Thanks and have a great day!</p>
          </td>
        </tr>
      </tbody>
    </table>

  </body>
